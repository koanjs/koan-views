'use strict';

/**
 * Dependencies
 */
var should = require('should');
var request = require('supertest');
var path = require('path');
var views = require('../');

describe('View renderer', function() {
  it('should render layoutless views');

  it('should render views with default layout when specified');

  it('should render views with default layout overriden');

  it('should work with layout specified without local variables');

  it('should work with layout specified without local variables');
});

